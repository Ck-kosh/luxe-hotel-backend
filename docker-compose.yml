version: '3.8'

services:
  backend:
    build: .
    container_name: luxe-hotel-api
    ports:
      - "8000:8000"
    volumes:
      - ./bookings.db:/app/bookings.db
    # env_file:
    #   - .env
    command: uvicorn App:app --host 0.0.0.0 --port 8000 --reload